export const metadata = {
  title: "HackWave Flood Engine",
  description: "Hybrid AI flood intelligence dashboard",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
